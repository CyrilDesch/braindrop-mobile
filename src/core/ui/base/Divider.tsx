import React from "react";

import { ThemedDivider, ThemedDividerProps } from "./Themed";

export function Divider(props: ThemedDividerProps) {
  return <ThemedDivider {...props} />;
}

export default Divider;
